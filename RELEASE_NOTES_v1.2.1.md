# BR-BookSystem 1.2.1

Packaging hotfix for BoxMate and manual installs.

## Fixed

- Added the required `boxroomplus` AssetBundle to `Mods/boxroomplus` in the release ZIP.
- Updated the project deployment target so normal Release deployments include the AssetBundle.
- Added a repeatable release packager that verifies every required runtime file exists.

No gameplay code changed from 1.2.0.
